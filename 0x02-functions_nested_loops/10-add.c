#include "main.h"
/**
 * add - Entry point
 * Description: This adds two integers and returns the result
 * @i: input number as an int
 * @j: input numer as an int
 * Return: sum of integers
 */
int add(int i, int j)
{
return (i + j);
}
