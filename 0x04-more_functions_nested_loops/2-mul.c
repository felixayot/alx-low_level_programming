#include "main.h"
/**
 * mul - Entry point
 * Description: This  multiplies two integers.
 * @a: input number as an int
 * @b: input number as an int
 * Return: product
 */
int mul(int a, int b)
{
return (a * b);
}
